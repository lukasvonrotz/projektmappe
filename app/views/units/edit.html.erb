<h1>Unit bearbeiten</h1>
<% if @unit.errors.any? %>
    <div class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
      <%= @unit.errors.full_messages.first %>
    </div>
<% end %>

<% subsystem = @unit.subsystem %>
<% project = subsystem.project %>

<%= render 'form', :unit => @unit,
           :subsystemid => @unit.subsystem.id,
           :context => false  %>

<%= link_to "Zurück zu Teilanlage", project_subsystem_path(project.id, subsystem.id) %>

<%= render 'layouts/loading', :message => "Lade..." %>