<h1>Neue Unit erstellen</h1>
<% if @unit.errors.any? %>
    <div class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
      <%= @unit.errors.full_messages.first %>
    </div>
<% end %>

<% subsystemid = @unit.subsystem_id.nil? ? params[:subsystem_id] : @unit.subsystem_id %>
<% subsystem = Subsystem.find(subsystemid) %>

<%= render 'form', :unit => @unit,
           :subsystemid => subsystemid,
           :context => true  %>

<%= link_to "Zurück zu Teilanlage", project_subsystem_path(subsystem.project.id, subsystem.id) %>

<%= render 'layouts/loading', :message => "Lade..." %>