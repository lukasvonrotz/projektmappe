<h1>Neue Offerte erstellen</h1>
<% if @offer.errors.any? %>
    <div class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
      <%= @offer.errors.full_messages.first %>
    </div>
<% end %>

<% subsubproject = @offer.subsubproject_id.nil? ? Subsubproject.find(params[:subsubproject_id]) : Subsubproject.find(@offer.subsubproject.id) %>

<%= render 'form', :offer => @offer,
           :subsubprojectid => subsubproject.id,
           :device_anzahl => 0,
           :userid => current_user.id,
           :context => true %>


<a href="/projects/<%= subsubproject.subproject.project.id %>/subprojects/<%= subsubproject.subproject.id %>/subsubprojects/<%= subsubproject.id %>/offerte">Zurück zu Offertberechnung</a>

<%= render 'layouts/loading', :message => "Lade..." %>